{
    "name": "api",
    "scripts": {
        "db:deploy": "npx prisma migrate deploy",
        "dev:build": "tsc -w",
        "dev:run": "nodemon --env-file=.env --delay 100ms --watch dist dist/index.js",
        "dev": "tsc && concurrently npm:dev:*",
        "build": "npx prisma generate && tsc",
        "start": "node --env-file=.env dist/index.js",
        "sample": "node --env-file=.env dist/scripts/add-sample-data.js",
        "seed": "node --env-file=.env dist/scripts/seed.js",
        "reset-seed": "npm run build && npx prisma db push --force-reset && npm run seed && npm run sample "
    },
    "peerDependencies": {
        "typescript": "^5.8.2"
    },
    "dependencies": {
        "@prisma/client": "^6.5.0",
        "@types/node": "^22.13.14",
        "concurrently": "^9.1.2",
        "fastify": "^5.2.1",
        "node-cron": "^3.0.3",
        "nodemon": "^3.1.9",
        "prisma": "^6.5.0",
        "zod": "^3.24.1"
    },
    "devDependencies": {
        "@types/node-cron": "^3.0.11"
    }
}
